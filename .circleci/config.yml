version: 2.1
jobs:
  ECS:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: calling makefile
          command: |
            echo $CIRCLE_USERNAME
            echo $CIRCLE_USERNAME


workflows:
  workflow_test:
    jobs:
      - ECS:
          context: AWSNCLOUDS
