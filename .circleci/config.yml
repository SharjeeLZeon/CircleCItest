version: 2.1
jobs:
  install_aws_cli:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: install packages
          command: |
            make one
            make two


workflows:
  workflow_test:
    jobs:
      - install_aws_cli