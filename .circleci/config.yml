version: 2.1
jobs:
  install_aws_cli:
    docker:
      - image: cimg/base:2021.04
    steps:
      - run:
          command: |
            sudo make install_awscli


workflows:
  workflow_test:
    jobs:
      - install_aws_cli