version: 2.1
jobs:
  ECS:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: "Running the file"
          command: |
            python3 main.py
      - run:
          name: calling makefile
          command: |
            make pip_install



workflows:
  workflow_test:
    jobs:
      - ECS:
          context: AWS