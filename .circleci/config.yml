version: 2.1
jobs:
  ECS:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: calling makefile
          command: |
            make install_awscli
            make push_image
            make register_task_definition
            make deploy_ecs
            


workflows:
  workflow_test:
    jobs:
      - ECS:
          context: AWS